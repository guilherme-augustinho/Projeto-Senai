import { Text, StyleSheet } from "react-native-web";



export function Condominio(props)
{
    return(
        <Text style = {styles.textA}>Opa</Text>
    );
}

const styles = StyleSheet.create({
    textA: {
        color: 'red'
    }
});